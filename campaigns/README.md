# Campaigns Directory

This directory stores all generated campaigns, including their history files, draft characters, and final outputs (`campaign_setting.md`, `current_state.md`, `{character_id}.json`).

Because these are generated artifacts and test data, the contents of this folder are ignored by Git, with the exception of this README.
